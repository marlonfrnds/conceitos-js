/*
    4. A variável student abaixo é de que tipo de dado?
        * atribua a ela propriedades e valores
        * mostre no console a seguinte mensagem <name> de idade <age> pesa <weight> kg
        * substitua <name> <age> e <weight> pelos valores de cada propriedade do objeto
*/

let student = {
    name: 'John Doe',
    age: 22,
    weight: 54.5,
}

console.log(typeof student);

console.log(`${student.name} de idade ${student.age} pesa ${student.weight} kg`)
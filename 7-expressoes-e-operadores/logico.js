/*
    Operadores lógicos

    2 valores booleanos, quando verificados,
    resultará em verdadeiro ou falso

    && AND 
    || OR
    !  NOT
*/

let pao = true
let queijo = true

console.log(pao && queijo);

console.log(pao || queijo);

console.log(!pao);
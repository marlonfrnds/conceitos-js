/*
    Operador condicional ternário

    Dependendo da condição, recebe valores
    diferentes

    ? SE
    : SENÃO
*/

let pao = true
let queijo = false

const niceBreakFast = pao && queijo ? 'Café top' : 'Café ruim'
console.log(niceBreakFast);

let age = 22
const canDrive = age >= 18 ? 'can drive' : "can't drive"
console.log();